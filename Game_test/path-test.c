#include "path-test.h"

#include "../Game/path.h"

#include <stdio.h>

#define TEST(name) { (char*)#name, name, NULL, NULL, MUNIT_TEST_OPTION_NONE, NULL}

static MunitResult intializePath_not_null(const MunitParameter params[], void* data)
{
	// Arrange

	// Assume

	// Assert

	return MUNIT_OK;
}